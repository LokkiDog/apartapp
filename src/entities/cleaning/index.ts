export type Cleaning = { id: string; status: string; scheduledOn: string; isUrgent: boolean; urgencyOverride: boolean | null; stayId: string | null; stay?: { adultCount: number; childCount: number } | null; checklist: Array<{ label: string; checked: boolean }>; comment?: string; hasProblem: boolean; problemDescription: string; completedAt?: string | null; apartmentId: string; apartment: { name: string; managers: Array<{ id: string; name: string }>; hotel: { name: string; address: string; latitude: string; longitude: string } }; assignments: Array<{ routePosition: number; cleanerId: string; cleaner: { id: string; name: string } }>; tariffSnapshot: { cleanerPoolEur?: number; ownerTotalEur?: number; laundryEur?: number; serviceEur?: number } }
