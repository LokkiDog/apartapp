type LinenPlanEntry = { source: 'booking' | 'type_default'; stayId: string | null; checkInOn: string | null; adultCount: number | null; childCount: number | null; guestCount: number }
export type Cleaning = { id: string; status: string; scheduledOn: string; isUrgent: boolean; urgencyOverride: boolean | null; stayId: string | null; stay?: { adultCount: number; childCount: number } | null; checklist: Array<{ label: string; checked: boolean }>; comment?: string; hasProblem: boolean; problemDescription: string; problems: Array<{ id: string; description: string; details: string; origin?: string; attachments: Array<{ id: string; fileName: string }> }>; linenPlan?: { current: LinenPlanEntry; prepared: LinenPlanEntry | null; mismatch: boolean; monitoring: boolean }; startedAt?: string | null; completedAt?: string | null; linenCollected: boolean; apartmentId: string; apartment: { name: string; instructions: string; managers: Array<{ id: string; name: string }>; hotel: { name: string; address: string; latitude: string; longitude: string } }; assignments: Array<{ routePosition: number; cleanerId: string; acceptedAt: string | null; cleaner: { id: string; name: string } }>; tariffSnapshot: { cleanerPoolEur?: number; ownerTotalEur?: number; laundryEur?: number; serviceEur?: number } }
export { default as LinenStatusControl } from './ui/LinenStatusControl.vue'
